---
title: "Mask Data"
format: html
toc: true
---

Mask data represent **regions** in each frame rather than single coordinates.

## How Mask Data Is Represented

In Neuralyzer, a mask is represented as a **binary spatial region** for a given frame/time index:

- Pixels inside the object are foreground.
- Pixels outside the object are background.
- A feature can have different mask geometry at different times.

This makes masks suitable when full object shape matters (for example tongue/body outlines, contact patches, segmented structures), not just point locations.

## How Masks Appear in the Media Viewer

When a mask feature is enabled, the Media Viewer draws it as an overlay on top of the media.

- By default, mask **alpha is 1.0** (fully opaque).
- **Color and alpha** can be adjusted in the Mask widget for visibility and contrast.
- Multiple mask features can be viewed together for comparison.
- The rendered overlay is display-focused; exported data keeps the mask geometry representation.

## Editing Masks in the Media Viewer

In **Mouse Mode = Brush**, you can directly edit masks in the media window:

- **Left click / drag** adds pixels to the mask.
- **Right click / drag** erases pixels from the mask.
- **Brush Size** controls edit radius, and you can optionally show the brush circle while hovering.
- You can change mask **color**, **alpha**, and additional display modes such as **Show bounding box**, **Show outline**, and **Use as transparency layer**.

## Common Follow-up Transforms

Mask data is often transformed into derived features:

- [Calculate Mask Area](../data_transformations/mask_area.qmd)
- [Calculate Mask Centroid](../data_transformations/mask_centroid.qmd)
- [Calculate Mask Principal Axis](../data_transformations/calculate_mask_principal_axis.qmd)
- [Skeletonize Mask](../data_transformations/skeletonize_mask.qmd)

## Practical Tip

Use masks when your downstream analysis depends on **shape**, **coverage**, or **boundary geometry**. Use points when you only need one tracked location.
